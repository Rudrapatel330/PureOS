[package]
name = "pureos-rust"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
bitflags = "2.4"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 2
